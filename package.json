{
	"name": "aidlc-workflows-docs",
	"type": "module",
	"private": true,
	"scripts": {
		"dev": "blume dev",
		"build": "blume build",
		"preview": "blume preview",
		"check": "bun run lint && bun run test && bun run validate:content && bun run build",
		"lint": "biome check .",
		"test": "vitest run",
		"test:e2e": "playwright test",
		"validate:content": "bun scripts/validate-content.ts",
		"sync": "bun scripts/sync-upstream.ts"
	},
	"dependencies": {
		"blume": "^1.0.3"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.5.3",
		"@playwright/test": "^1.61.1",
		"@types/bun": "latest",
		"github-slugger": "^2.0.0",
		"typescript": "6",
		"vitest": "^4.1.10",
		"yaml": "^2.6.0",
		"zod": "^3.24.0"
	},
	"patchedDependencies": {
		"@astrojs/mdx@7.0.3": "patches/@astrojs%2Fmdx@7.0.3.patch"
	}
}
